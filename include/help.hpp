#pragma once

namespace mopwd {
inline constexpr char HELP_TEXT[] = R"(Usage: mopwd [OPTION]

Print the current working directory.

Options:
    -L, --logical   print logical path
    -P, --physical  print physical path
    -h, --help      display this help
    -v, --version   display version
)";
}
